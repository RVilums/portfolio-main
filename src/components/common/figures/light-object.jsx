import "./light-object.styles.scss";

const LightObject = (props) => {
  let lightObject;

  lightObject = <div className="sun"></div>;

  return lightObject;
};

export default LightObject;
